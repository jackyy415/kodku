# Kodku proof — WhatsApp / Excel in, table out

Hong Kong SMEs still run jobs through Excel, WhatsApp, and email. The list lives in a group chat; the spreadsheet is a week behind; nobody wants a “system.”

This folder is a one-workflow slice: paste a WhatsApp export or a CSV, get a searchable table, export it. It runs in the browser. No login, no backend, no monthly fee. That is the $800 Kodku slice. The $1,500 build is the same idea plus handover so staff can run it themselves.

Kodku also takes Java/backend tickets as a 10-hour block at $60–80/hr ($600–800). Evenings and weekends. Invoice as Kodku. Email info@kodku.com.

Open `index.html` for the two offers. Open `demo.html` for the working slice. Do not publish until Jacky/CoS approve. Do not use jackyy415@gmail.com on this site.
